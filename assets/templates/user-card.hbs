<div class="img" data-uri="{{user.uri}}">
    <img src="{{user.images.0.url}}">
</div>
<div class="card-body">
    <span class="text-muted">You are logged in as:</span>
    <h5>
        {{user.display_name}}
        <span class="badge badge-success">{{user.followers.total}}</span>
    </h5>
</div>